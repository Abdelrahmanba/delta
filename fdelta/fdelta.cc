#include "fdelta.h"

uint64_t fencode(uint8_t* inputBuf, uint64_t inputSize,
                  uint8_t* baseBuf, uint64_t baseSize,
                  uint8_t** outputBuf, uint64_t* outputSize) {
    
}
